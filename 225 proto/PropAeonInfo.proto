syntax = "proto3";

option java_package = "emu.lunarcore.proto";

message PropAeonInfo {
    // 时空守护者ID
    uint32 aeon_id = 13;
    // 对话组ID
    uint32 dialogue_group_id = 11;
    // 增加经验值
    uint32 add_exp = 4;
}